<!--
   - Resin 4.0 configuration file.
   -
   - See conf/full.xml for more sample configuration, including load
   - balancing and database configuration.
  -->
<resin xmlns="http://caucho.com/ns/resin"
       xmlns:resin="urn:java:com.caucho.resin">

  <!--
     - Logging configuration for the JDK logging API.
    -->
  <!--log-handler name="" level="all" path="stdout:"
               timestamp="[%y-%m-%d %H:%M:%S.%s] {%{thread}} "/-->

  <stdout-log path='/data/soft/resin/log/stdout.log' rollover-period='1D' rollover-size='50mb'/>
  <stderr-log path='/data/soft/resin/log/stderr.log' rollover-period='1D' rollover-size='50mb'/>

  <!--
     - level='info' for production
     - 'fine' or 'finer' for development and troubleshooting
    -->
  <logger name="com.caucho" level="info"/>

  <logger name="com.caucho.java" level="config"/>
  <logger name="com.caucho.loader" level="config"/>

  <!--
     - driver .jar files can be placed in ${resin.root}/ext-lib
     -->
  <class-loader>
    <tree-loader path="${resin.root}/ext-lib"/>
  </class-loader>
  
  <!--
     - Remote management requires at least one enabled admin user.
    -->
  <resin:AdminAuthenticator>
    <resin:import path="${__DIR__}/admin-users.xml" optional="true"/>
  </resin:AdminAuthenticator>

  <!--
     - For production sites, change dependency-check-interval to something
     - like 600s, so it only checks for updates every 10 minutes.
    -->
  <dependency-check-interval>2s</dependency-check-interval>

  <!-- Java system-properties -->
  <system-property mail.smtp.host="127.0.0.1"/>
  <system-property mail.smtp.port="25"/>
  <system-property jmagick.systemclassloader='no'/> 
  <!--
     - default configuration applied to all clusters
    -->
  <cluster-default>
    <!-- standard servlet behavior, including .jsp, .php, and WEB-INF -->
    <resin:import path="${__DIR__}/app-default.xml"/>

    <!-- enables development error pages -->
    <development-mode-error-page/>

    <!--
       - Enables the proxy cache (available in Resin Professional) 
      -->
    <resin:if test="${resin.professional}">
      <cache memory-size="128M">
       <!-- Vary header rewriting for IE -->
        <rewrite-vary-as-private/>
      </cache>
    </resin:if>
    
    <!--
       - Admin services
      -->
    <resin:DeployService/>
    
    <resin:if test="${resin.professional}">
      <resin:AdminServices/>
    </resin:if>

    <!--
       - Default host configuration applied to all virtual hosts.
      -->
    <host-default>
      <access-log path="log/access.log" 
                format='%h %l %u %t "%r" %s %b "%{Referer}i" "%{User-Agent}i"'
                rollover-period="1W"/>

      <!-- creates the webapps directory for .war expansion -->
      <web-app-deploy path="webapps" versioning="true"/>

      <!--
         - Defaults applied to each web-app.
        -->
      <web-app-default>
        <prologue>
          <allow-servlet-el/>
        </prologue>

        <!-- enable persistent sessions -->
        <session-config>
          <use-persistent-store/>
          <enable-url-rewriting>false</enable-url-rewriting>
        </session-config>
     
        <!--
           - Sets max-age for cacheable pages, e.g. static pages.
          -->
        <resin:if test="${resin.professional}">
          <cache-mapping url-pattern="/" max-age="5s"/>
          <cache-mapping url-pattern="*.gif" max-age="60s"/>
          <cache-mapping url-pattern="*.jpg" max-age="60s"/>
          <cache-mapping url-pattern="*.png" max-age="60s"/>
          <cache-mapping url-pattern="*.css" max-age="60s"/>
          <cache-mapping url-pattern="*.js" max-age="60s"/>
         </resin:if>
      </web-app-default>
    </host-default>
  </cluster-default>
<cluster id="MicloudAPI">
    <!-- sets the content root for the cluster, relative to resin.root -->
    <root-directory>.</root-directory>

    <!-- defaults for each server, i.e. JVM -->
    <server-default>
      <!-- The http port -->
      <http address="*" port="8090"/>
 
      <jvm-arg>-Xms128M</jvm-arg>
      <jvm-arg>-Xmx2048M</jvm-arg>
      <!--
         - If starting Resin as root on Unix, specify the user name
         - and group name for the web server user.
       <user-name>www-data</user-name>
       <group-name>www-data</group-name>
        -->
    </server-default>

    <!-- define the servers in the cluster -->
    <server id="MicloudAPI" address="127.0.0.1" port="6810">
    </server>

      
    <host id="" root-directory=".">
      <web-app id="/" root-directory="/opt/www/xiaomi_MicloudAPI/"/>
      <stdout-log path='/data/soft/resin/log/MicloudAPI.stdout.log' rollover-period='1D' rollover-size='50mb'/>
      <stderr-log path='/data/soft/resin/log/MicloudAPI.stderr.log' rollover-period='1D' rollover-size='50mb'/>
    </host>

  </cluster>
<cluster id="MicloudAPI-SMS">
    <!-- sets the content root for the cluster, relative to resin.root -->
    <root-directory>.</root-directory>

    <!-- defaults for each server, i.e. JVM -->
    <server-default>
      <!-- The http port -->
      <http address="*" port="8092"/>
 
      <jvm-arg>-Xms128M</jvm-arg>
      <jvm-arg>-Xmx2048M</jvm-arg>
      <!--
         - If starting Resin as root on Unix, specify the user name
         - and group name for the web server user.
       <user-name>www-data</user-name>
       <group-name>www-data</group-name>
        -->
    </server-default>

    <!-- define the servers in the cluster -->
    <server id="MicloudAPI-SMS" address="127.0.0.1" port="6812">
    </server>

      
    <host id="" root-directory=".">
      <web-app id="/" root-directory="/opt/www/xiaomi_MicloudAPI-SMS/"/>
      <stdout-log path='/data/soft/resin/log/MicloudAPI-SMS.stdout.log' rollover-period='1D' rollover-size='50mb'/>
      <stderr-log path='/data/soft/resin/log/MicloudAPI-SMS.stderr.log' rollover-period='1D' rollover-size='50mb'/>
    </host>

  </cluster>
<cluster id="MicloudWeb">
    <!-- sets the content root for the cluster, relative to resin.root -->
    <root-directory>.</root-directory>

    <!-- defaults for each server, i.e. JVM -->
    <server-default>
      <!-- The http port -->
      <http address="*" port="8080"/>
 
      <jvm-arg>-Xms128M</jvm-arg>
      <jvm-arg>-Xmx2048M</jvm-arg>
      <!--
         - If starting Resin as root on Unix, specify the user name
         - and group name for the web server user.
       <user-name>www-data</user-name>
       <group-name>www-data</group-name>
        -->
    </server-default>

    <!-- define the servers in the cluster -->
    <server id="MicloudWeb" address="127.0.0.1" port="6800">
    </server>

      
    <host id="" root-directory=".">
      <web-app id="/" root-directory="/opt/www/xiaomi_MicloudWeb/"/>
      <stdout-log path='/data/soft/resin/log/MicloudWeb.stdout.log' rollover-period='1D' rollover-size='50mb'/>
      <stderr-log path='/data/soft/resin/log/MicloudWeb.stderr.log' rollover-period='1D' rollover-size='50mb'/>
    </host>

  </cluster>
  <cluster id="passport-http">
    <!-- sets the content root for the cluster, relative to resin.root -->
    <root-directory>.</root-directory>

    <!-- defaults for each server, i.e. JVM -->
    <server-default>
      <!-- The http port -->
      <http address="*" port="8102"/>

      <jvm-arg>-Xms256M</jvm-arg>
      <jvm-arg>-Xmx2048M</jvm-arg>
      <!--
         - If starting Resin as root on Unix, specify the user name
         - and group name for the web server user.
       <user-name>www-data</user-name>
       <group-name>www-data</group-name>
        -->
    </server-default>
          
    <!-- define the servers in the cluster -->
    <server id="passport-http" address="127.0.0.1" port="6822">
    </server>

           
    <host id="" root-directory=".">
      <web-app id="/" root-directory="/opt/www/xiaomi_passport-http/"/>
      <stdout-log path='/data/soft/resin/log/Passport.stdout.log' rollover-period='1D' rollover-size='50mb'/>
      <stderr-log path='/data/soft/resin/log/Passport.stderr.log' rollover-period='1D' rollover-size='50mb'/>
    </host>
          
  </cluster>
<cluster id="passport-https">
    <!-- sets the content root for the cluster, relative to resin.root -->
    <root-directory>.</root-directory>
                
    <!-- defaults for each server, i.e. JVM -->
    <server-default>
      <!-- The http port -->
      <http address="*" port="8100"/>
      
      <jvm-arg>-Xms256M</jvm-arg>
      <jvm-arg>-Xmx2048M</jvm-arg>
      <jvm-arg>-XX:MaxPermSize=128M</jvm-arg>
      <!--
         - If starting Resin as root on Unix, specify the user name
         - and group name for the web server user.
       <user-name>www-data</user-name>
       <group-name>www-data</group-name>
        -->
    </server-default>
          
    <!-- define the servers in the cluster -->
    <server id="passport-https" address="127.0.0.1" port="6820">
    </server>
        
           
    <host id="" root-directory=".">
      <web-app id="/" root-directory="/opt/www/xiaomi_passport-https/">
        <resin:Forbidden regexp="/views"/>
      </web-app>
      <stdout-log path='/data/soft/resin/log/Passport.stdout.log' rollover-period='1D' rollover-size='50mb'/>
      <stderr-log path='/data/soft/resin/log/Passport.stderr.log' rollover-period='1D' rollover-size='50mb'/>
    </host>
          
  </cluster>

  <cluster id="nginx-access-tracker">
    <!-- sets the content root for the cluster, relative to resin.root -->
    <root-directory>.</root-directory>
                
    <!-- defaults for each server, i.e. JVM -->
    <server-default>
      <!-- The http port -->
      <http address="*" port="8118"/>
      
      <jvm-arg>-Xms256M</jvm-arg>
      <jvm-arg>-Xmx2048M</jvm-arg>
      <jvm-arg>-XX:MaxPermSize=128M</jvm-arg>
      <!--
         - If starting Resin as root on Unix, specify the user name
         - and group name for the web server user.
       <user-name>www-data</user-name>
       <group-name>www-data</group-name>
        -->
    </server-default>
          
    <!-- define the servers in the cluster -->
    <server id="nginx-access-tracker" address="127.0.0.1" port="6828">
    </server>
        
           
    <host id="" root-directory=".">
      <web-app id="/" root-directory="/opt/www/xiaomi_nginx-access-tracker/"/>
      <stdout-log path='/data/soft/resin/log/nginx-access-tracker.stdout.log' rollover-period='1D' rollover-size='50mb'/>
      <stderr-log path='/data/soft/resin/log/nginx-access-tracker.stderr.log' rollover-period='1D' rollover-size='50mb'/>
    </host>
          
  </cluster>

  <cluster id="UserDeviceAPI">
    <!-- sets the content root for the cluster, relative to resin.root -->
    <root-directory>.</root-directory>

    <!-- defaults for each server, i.e. JVM -->
    <server-default>
      <!-- The http port -->
      <http address="*" port="8200"/>

      <jvm-arg>-Xms256M</jvm-arg>
      <jvm-arg>-Xmx2048M</jvm-arg>
      <jvm-arg>-XX:MaxPermSize=128M</jvm-arg>
      <!--
         - If starting Resin as root on Unix, specify the user name
         - and group name for the web server user.
       <user-name>www-data</user-name>
       <group-name>www-data</group-name>
        -->
    </server-default>

    <!-- define the servers in the cluster -->
    <server id="UserDeviceAPI" address="127.0.0.1" port="6832">
    </server>


    <host id="" root-directory=".">
      <web-app id="/" root-directory="/opt/www/xiaomi_UserDeviceAPI/"/>
      <stdout-log path='/data/soft/resin/log/UserDeviceAPI.stdout.log' rollover-period='1D' rollover-size='50mb'/>
      <stderr-log path='/data/soft/resin/log/UserDeviceAPI.stderr.log' rollover-period='1D' rollover-size='50mb'/>
    </host>

  </cluster>

</resin>

